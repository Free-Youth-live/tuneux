# 变更日志

本文件记录 tuneux 各版本的变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [0.5.1] - 2026-09-23

### 变更

- **界面配色全插件化（tuneux-fx）**：删除内置主题（DOS / 干净）与 F2 切换、`:theme` 命令、菜单「配色切换」；调色板只来自皮肤插件——随包第一方「午夜蓝」皮肤（官方签名，plugins/ 三件套），验签 / 解析失败静默回落终端原生配色
- **移除「打开文件 / 打开目录」**：F3 / F4、菜单项与 `:open-file` / `:open-dir` 命令整体删除——文件浏览器（b 键）+ `/` 递归搜索 + `a` 加入目录完整覆盖，浏览器为唯一入口
- F1 帮助键删除（与 `?` 重复）；帮助菜单两项（快捷键速查/关于、关于）合并为一项「关于 / 快捷键速查」；功能键栏精简为 1-8菜单 / F5-F8面板 / F9均衡器 / F10菜单 / ?帮助
- **CUE 解析扩展**（corex cue.rs）：新增 `CueSheet`（专辑标题/表演者/FILE 引用）与 `parse_cue_sheet`；`CueTrack` 增 INDEX 00 间隙起点、终点推算、轨迹类型（数据轨标记不可播）、起始扇区换算；`decode_cue_bytes` 编码链（UTF-8 → BOM → GB18030）自双发行版下沉 corex
- **双发行版 CUE 分轨播放改走区间播放**：终点判定下沉引擎（重采样前按文件采样率累计），前端 position 轮询与切曲守卫退役；同文件相邻分轨经区间预载无缝衔接
- **cue+bin 光盘镜像播放**：浏览器列出 `.cue` 文件，选中即按 cue 的 FILE 引用展开分轨（FILE 指向 `.bin` 时经自研 CDDA 后端解码）；整轨旁同名 `.cue` 的老路径照常；数据轨自动跳过并提示条数

### 新增

- **自研解码三阶段（无损优先）**：`.wav` 与 `.flac` 解码路径接管为纯 Rust 手写后端（流式、零新增依赖；不支持的形态自动回退 symphonia，存量文件零退化）；`.wv`（WavPack）解码改为自研实现（第三方 wavicle 退出生产依赖，仅留测试对拍参照物）——含往返对拍与真实语料位级校验
- **区间播放 `AudioCmd::PlayRange`**（corex 0.3.1）：只播放文件中的 [start_secs, end_secs) 区间——终点按源文件时间轴在重采样前累计判定、尾巴播完再上报结束；时长与进度按区间上报（cue 分轨 / 光盘镜像单曲目 / 外部消费场景）
- **`PreloadTarget` 预载目标结构化**：区间预载（后端先 seek 到起点再缓存，同文件相邻区间无缝衔接）——**破坏性变更**：`AudioCmd::PreloadNext` 的参数由 `Option<PathBuf>` 改为 `Option<PreloadTarget>`（corex 未到 1.0，随 0.3.1 标注）
- **CD-DA 镜像（.bin）自研解码后端**：2352/2448 扇区探测（DAO-96 子通道剥离）、流式扇区读取、构造泛型支持内存源（`Cursor`）；`.bin` 不进已知扩展名清单（入口由 .cue 引导）
- 循环播放模式（RepeatMode）下沉共享数据层（tuneux-mediax）：序列化表示冻结（"off"/"single"/"list"），双端旧配置直读、无需迁移
- 时间分解函数下沉共享数据层；基础版状态栏与播放列表时长显示统一取整与小时位口径（超 1 小时显示 h:mm:ss）

### 修复与加固

- **FLAC 解码还原 wasted bits**：子帧声明 wasted 时样本最低 k 位须左移还原，旧实现只扣有效位深不左移——16bit 母带封装进 24bit 容器（k=8）之类的**合法文件整体衰减 2^k 倍**，且帧 CRC 覆盖原始字节、发现不了；补位流级回归测试
- **均衡器 Nyquist 保护**：`f0 ≥ sr/2` 时 peaking 系数极点出单位圆、输出发散为 Inf/NaN（22.05k / 24k / 11.025k / 12k 流下的 16k / 8k 段），越界段改单位系数直通，出口再加非有限值兜底
- **压缩器参数拒绝非有限值**：五个 setter 原只做 `clamp`，而 `f32::clamp` 对 NaN 原样返回——插件可经 `compressor_set` 把 NaN 送进音频线程；现入口拒绝（返回 `false`、不改原值）+ 出口兜底，与均衡器同口径
- **插件验签改严格模式**：普通 `verify` 不拒小阶（弱）公钥，可自选弱公钥伪造"验签通过"；改 `verify_strict` + `is_weak` 双重防护
- **插件沙箱内存 / 表配额补个数上限**：wasmi 的 `memory_size` / `table_elements` 是**逐件**上限，多内存模块可把 4 页配额放大到 4 万页（≈2.5 GiB）；内存 / 表各限 1 个后，逐件上限即等价于 store 级上限
- **配置与播放状态改原子写**（临时文件 + rename，防强杀留下半截文件）；基础版播放状态补 `serde(default)`（缺键不再让整份状态回退默认、播放列表与断点静默清空）
- **设备热切换**：暂停态改发 `LoadAndSeek` 保留进度（原先无视播放态一律从头加载）；进度按**旧**流采样率换算（44.1k→48k 场景原先偏小约 15 秒）；重建流的通道数保持启动时取值（解码线程的通道适配 / 交错 / 重采样均基于该值，跟随新设备会变速变调）
- **ReplayGain**：seek 后不再写出残缺测量值（跳过的中段不参与统计，残缺值会被按路径缓存并跨曲次回灌）；测量哨兵与合法值域解耦
- **解码器健壮性**：WavPack 尾部标签宽容（非 `wvpk` 魔数视为流结束）；CD-DA 扇区尺寸互素歧义加同步模式二次确认（扇区数为 49 的倍数时 2448 曾被误判为 2352、整轨爆音）；CUE `start_sector` 改整数运算（消浮点截断，原先 5.34% 的 MM:SS:FF 少一扇区）；FLAC seek 偏移越界回退线性解码；WAV `fmt` chunk 与 WavPack 块声明长度上限、WavPack 轻量探测块数上限（防无界分配与探测挂起）
- **区间播放**：seek 失败时不再重置区间基准、也不再作废响度分析器（失败即位置未变）
- **双端 TUI**：浏览器搜索不再污染播放列表面板（改传布局度量字段，修多画搜索框与滚动差一行）；搜索框在空列表 / 零匹配时保留渲染（消"按键被吞却无输入框"的隐形模态）；封面网格选中下标渲染时钳制、窄终端不再越界绘制；启动恢复剥离封面字节（防同专辑重复占用 GB 级内存）；分组序号改预扫描（滚动后不再从 1 重算）；F10 唤起后 Enter 先展开下拉（不再盲执行菜单项）；插件核查日志 id 逐插件唯一（修 first_seen 失真）
- 注释口径收口：播放序 / 预载守卫 / 声道重建 / 直通策略 / seek 策略 / 配置文件名 / 延迟线长度 / FFT 口径等 13 处与实现不符的注释更正；内部阶段编号、日期戳与规范编号清出公开注释
- 文档与品牌：README、两份使用手册与「关于」弹窗为产品名补 `tuneux™`（`tuneux-fx` 及所有带连字符的名称保持裸名）；fx「关于」收尾改为「商标行 + 许可 / 版权」排列
- 均衡器 / 压缩器立体声缓冲对齐断言（上游帧对齐不变式显式化）
- 删除双端从未接入任何按键 / 菜单的排序接口（历史遗留）
- 插件生成工具改为只写指定目标（防整目录重生成引发连锁重签）
- 生产路径零 unwrap 进入编译检查（workspace lints）；推送扫描补内部代号与用词规范禁用词、提交说明扫描覆盖正文；发布流程增加依赖核查关卡

### 补记（0.5.0 遗漏）

- 0.5.0 未记条目在此补录：基础版文件浏览器异步导航 / 浏览器搜索目录树收集异步化 / 目录递归加入异步化（含完成提示与空列表自动首播）/ Windows 隐藏与系统目录过滤 / 音频设备探针示例 / ReplayGain 测量增益符号丢失修复 / 搜索态 Enter 文件残留修复 / 上次目录过期修复 / 搜索收集期键入清空列表修复 / 基础版浏览器去重命中行为对齐

## [0.5.0] - 2026-09-07

### 新增

- **插件系统**（tuneux-pinx + tuneux-fx）：WASM 插件宿主（wasmi 解释器沙箱 + 燃料/内存配额）、能力制（默认拒绝 + 求交 + 网络⟂内容读取互斥）、Ed25519 验签三态（已认证 / 已签名·作者未知 / 未签名）
- **第一方插件**：均衡器（10 段 peaking）与压缩器（峰值检测 + 包络跟随）；DSP 全在 corex 音频线程，插件只做控制面写参数
- **动态多槽位**：均衡器 / 压缩器各 8 槽，插件按 slot 寻址，支持同类效果器叠加
- **插件目录加载**：第一方插件运行时读 `plugins/` 目录（插入↔拔出），官方公钥内嵌二进制验签，`.sig` 随包分发
- **发布双产物**：tuneux 包（二进制 + 使用手册-tuneux）、tuneux-fx 包（二进制 + 使用手册-tuneux-fx + plugins/ 插件目录）
- 基础版文件浏览器异步导航（后台线程读目录 + 代次计数丢弃陈旧结果），大目录 / 网络盘不再卡 UI
- 浏览器搜索 `/` 的目录树收集异步化（双端）：收集期间可继续输入关键字，结果到达后立即生效；大目录按 `/` 不再停留
- `a` 键加目录 / fx 打开目录（F4、`:open-dir`）异步化（双端）：递归遍历、CUE 展开、逐文件元数据探测全在后台线程，完成时提示加入数量，空列表自动播放第一首
- 双端 Windows 浏览器与递归扫描跳过「隐藏 / 系统」属性目录（$RECYCLE.BIN、System Volume Information 等）
- tuneux-corex 新增音频设备探针示例（`device_probe`：列出输出设备默认配置与采样率区间）

### 修复

- ReplayGain 测量增益跨线程读取丢失符号：响度高于目标的曲目（负增益）被读成极大正数，开启响度归一后被钳到 16 倍增益导致爆音削波
- fx 搜索态在浏览器 Enter 文件后不退出搜索（目录分支会退，行为不一致）
- 异步导航后立即退出时，保存的「上次目录」仍是旧目录
- 异步导航结果到达时若正处浏览器搜索态，连带干净退出搜索（避免搜索框与浏览器状态错位）
- 浏览器搜索进入后、目录树收集结果未到达期间键入关键字会把列表清成空白（双端）——收集期只记关键字不过滤，结果到达后按当前关键字过滤立即生效
- 基础版浏览器 Enter 已在列表的文件（去重命中）静默无反应——对齐 fx 行为，定位并立即播放

## [0.4.4] - 2026-08-22

### 新增

- 系统媒体键（Linux MPRIS / Windows 键盘钩子），终端后台也能控制播放
- ReplayGain 缓存持久化，重启免重复分析
- FFmpeg 后端 CI 集成验证
- DSD（DSF/DFF）经 ffmpeg 解码（前提是用户自己安装了ffmpeg）

### 修复

- 若干音频与播放列表问题（mono 上混衰减、排序缓存、MPRIS 语义等）

### 重构

- app.rs 与 engine_thread.rs 拆分重构，提升可维护性

## [0.4.3] - 2026-08-22

### 新增

- **FFmpeg 进程外后端完整实现**：ffmpeg 子进程解码（s16le PCM 管道）→ f32；seek 用 -ss 重启子进程；stderr 异步读取错误透传；ape/wma/tak 等长尾格式接入 DecoderBackend
- 降级：ffmpeg 未安装时返回 Unsupported（提示）；子进程异常退出时透传 stderr 错误

## [0.4.2] - 2026-08-22

### 新增

- **ReplayGain 音量标准化**（播放集成）：流式响度分析（播放中逐块分析整曲 LUFS，目标 -14 LUFS）→ 增益缓存 → 下次播放自动应用；曲目播完/无缝切曲均记录分析结果。首次播放无增益（分析中），第二次起生效
- 引擎：SharedState 增益应用（音量 × ReplayGain 叠加）、解码线程流式分析器（rg::LoudnessAnalyzer，与 measure_lufs 结果一致）

### 修复

- Gapless 无缝切曲时也保存 ReplayGain 分析结果（不因跳过 draining 丢失测量）

## [0.4.1] - 2026-08-22

### 新增

- **快捷键自定义**：配置 `keymap` 自定义播放控制键（toggle_play/next/prev/volume_up/volume_down）；非法映射自动剔除
- **设备热切换**：默认输出设备变化时自动重建流并重载当前曲目（含暂停态；重建后正确恢复播放/进度/直通标识）
- **系统媒体键方案**：Windows(windows crate) → Linux(zbus MPRIS) → macOS(objc2-media-player)，feature-flag 分平台落地
- **ffmpeg 进程外 backend 骨架**：FfmpegBackend 结构 + IPC 协议设计 + 扩展名分发（ape/wma/tak 等 symphonia 外格式）；骨架期返回明确的"未实现"错误（不静默跳曲）

### 修复

- ffmpeg 分支排除 symphonia 已支持格式（m4a 等回退进程内后端）
- 设备热切换：重建后显式 play()（消除永久静音）、暂停态也重建、进度基准清零、直通标识重算

## [0.4.0] - 2026-08-22

### 新增

- **可插拔解码后端**（DecoderBackend 抽象）：symphonia / Opus / WavPack 统一接入，为 ffmpeg 等预留
- **Opus 支持**：Ogg Opus 解封装 + 纯 Rust 解码器（零 unsafe 零 FFI）；granule 页级 seek（秒级定位）
- **WavPack 支持**：纯 Rust 解码（16/24/32 位 + 浮点）
- **Gapless 无缝播放**：下一曲预载 + EOF 无缝切换（同采样率零间隙；异采样率优雅降级）
- **ReplayGain 核心算法**（EBU R128 响度测量 + 增益计算）
- **内嵌歌词**：读取歌曲内嵌歌词标签（ID3 USLT / Vorbis LYRICS 等）
- **播放列表增强**：去重开关 + 排序 API

### 质量（P1/P2）

- / 状态机单测、断点续播测试、回调零分配化、布局单一真相源（LayoutMetrics）
- render.rs 拆分（1681 行 → 主入口 + 5 子模块）
- CUE seek 钳制、注释/allow 清理

## [0.3.0] - 2026-08-22

### 更名

- 项目、二进制名、配置文件（`tuneux.toml`）统一迁移为 tuneux
- 原地更名演进

### 架构

- 仓库改为 Cargo workspace 多 crate 结构（mono repo + `crates/*`）
- 抽取共享音频内核 **tuneux-corex v0.1.0**（解码 / 四线程引擎 / 重采样 / 频谱）
- 前端 tuneux 仅依赖 tuneux-corex；

### 修复

- 码率除零 panic（全程浮点 + panic hook + 回归测试）
-  EOF 结尾 2 秒截断（draining 排空协议 + Resample::flush + EOF 后 seek 恢复）
-  暂停丢包（is_playing 门控挂起解码 + push_all 帧对齐）

### 迁移提示

- 旧配置文件需手动迁移为 `tuneux.toml`

## [0.2.2] - 2026-08-16

### 新增

- 界面状态持久化：频谱、歌词、文件浏览器、专辑封面的开关状态退出后保留
- 随二进制分发使用说明 `使用手册-简版.txt`（快捷键、三系统注意事项、歌词与配置说明）

### 修复

- 启动后自动选中上次播放的歌曲（高亮落在它上，按 Enter 从上次位置续播）
- Windows 直接关闭窗口时配置与播放状态丢失（改为每 5 秒自动保存）

## [0.2.0] - 2026-08-16

### 新增

- 播放列表分组视图：`g` 键在平铺 / 按专辑分组间切换；组头显示「专辑 - 歌手 (N首) (总时间)」，歌手超过 3 个显示"群星"，组头可折叠/展开（Enter）
- 歌词：`l` 键显示/隐藏（播放列表右侧面板，可与频谱共存）；自动加载同目录同名 `.lrc`（UTF-8 / UTF-16 / GBK/GB18030 编码自动识别），当前行高亮随进度滚动
- 播放列表持久化：退出时自动保存到独立文件 `playlist.toml`（含列表 + 每首断点续播进度），下次启动自动恢复
- 播放列表管理：`d` 删除选中曲目，`x` 清空播放列表（按两次确认）
- Windows 文件浏览器支持跨盘符导航（盘符根目录列出其它盘符）

### 修复

- 随机播放时 `p` 键无法回到上一首（history 重复压栈）

## [0.1.0] - 2026-08-15（胜利）

首个发布版本。

### 新增

**播放核心**

- 音频解码：MP3 / FLAC / WAV / OGG / M4A / AAC / ALAC，基于 symphonia（100% 纯 Rust，无系统编解码器依赖）
- 原生采样率直通：设备支持时按文件原生采样率重建输出流，无软件重采样（bit-perfect）；不支持时自动以 rubato 高质量重采样降级，界面技术参数行以亮/暗颜色区分直通与降级状态
- 四线程音频架构：主线程（TUI）、音频线程（cpal Stream）、cpal 回调线程、常驻解码线程，以无锁环形缓冲桥接，实时回调零阻塞
- 断点续播：记录每首曲目的播放进度，重启后从上次位置继续

**界面（TUI，无子命令，直接运行进入）**

- 文件浏览器：`b` 键显隐，`/` 键递归搜索子目录
- 播放列表：加入目录后按"专辑-曲序"自动排序展示与播放，支持曲名/歌手/专辑/路径搜索
- 当前曲面板：标题、艺术家、专辑·曲序、编码格式·码率·采样率·位深
- 专辑封面：`c` 键显示标签内嵌封面（终端字符渲染）
- 实时频谱：`v` 键切换 关 → 半屏 → 全屏（FFT 驱动，256 对数频段）
- 实时电平表、`?` 键"关于"弹窗（版本号、简介、开源声明、版权）

**播放控制**

- 播放/暂停、下一曲/上一曲、单曲循环/列表循环、随机播放
- 音量调节、±5 秒快进快退、断点续播

**配置**

- 便携优先：配置存于可执行文件同目录（U 盘随身带）；不可写时自动回退系统配置目录
- 正常退出自动保存：音量、循环模式、随机开关、列宽、上次浏览目录、播放进度

**声明**

- 纯离线：无任何联网依赖，不收集数据
- 跨平台：Windows 10+ / macOS 10.13+ / Linux（主流发行版），x86_64 与 aarch64
